import { AccountsState } from "@/modules/authentication/store/accounts/state";
import { Account } from "@/modules/authentication/models/Account";
import { AuthProvider } from "@/modules/authentication/models/AuthProvider";

export default {
  // addAccount: (state: AccountsState, provider: AuthProvider) => {
    // const newAccount = new Account(provider);
    // state.accounts.push(newAccount);
    // return newAccount.id;
  // }
}